[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
vullm = { path = ".", editable = true }
# tree-sitter-extended-c = { path = "/Users/matteoguglielmi/dev/VuLLM/libs/tree-sitter-extended-c", editable = true }

[tool.pixi.environments]
default = { solve-group = "default" }
dev = { features = ["dev"], solve-group = "default" }
